# Railway deploy config for AI-CORE.
# Railway builds from Dockerfile. See README.md for env vars.

[deploy]
startCommand = "python -m uvicorn apps.orchestrator.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
